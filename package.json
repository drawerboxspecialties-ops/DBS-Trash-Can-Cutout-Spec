{
  "name": "trash-can-cutout-spec",
  "version": "1.0.0",
  "description": "Pullout trash can cutout spec calculator — box layout and CNC dimensions for shop floor",
  "private": false,
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "format": "prettier --write \"**/*.{js,css,html,md,json,yml}\"",
    "format:check": "prettier --check \"**/*.{js,css,html,md,json,yml}\"",
    "start": "npx --yes serve . -p 8765"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "cabinet",
    "cutout",
    "trash-can",
    "cnc",
    "woodworking"
  ],
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
